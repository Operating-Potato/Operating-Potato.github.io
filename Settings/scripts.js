const root = document.documentElement;

let darkSidebar = window.getComputedStyle(root);

let propertyValue = darkSidebar.getPropertyValue('sidebar-colour');

function toggleDark() {
    if 
}

                    root.style.setProperty('--sidebar-colour', 'rgba(0,0,0,0.5)');